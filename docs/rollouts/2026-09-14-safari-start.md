# 2026-09-14 — Host Safari start page at /start/

Board `970746b9`.  Branch `fx/safari-start`.

Copy of AFC `scripts/safari-start/index.html` at `site/public/start/index.html` so iPhone Safari can open `https://jays.services/start/` and Add to Home Screen.  Settings live in that page's `localStorage`.  Mac still uses the local `file://` install.
